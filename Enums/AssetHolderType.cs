namespace SFManagement.Enums;

/// <summary>
/// Represents the different types of asset holders in the system
/// </summary>
public enum AssetHolderType
{
    Unknown = 0,
    Client = 1,
    Bank = 2,
    Member = 3,
    PokerManager = 4
} 